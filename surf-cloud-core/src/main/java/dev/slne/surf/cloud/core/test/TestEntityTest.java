package dev.slne.surf.cloud.core.test;

import org.springframework.boot.CommandLineRunner;
import org.springframework.stereotype.Component;

@Component
public class TestEntityTest implements CommandLineRunner {

  public TestEntityTest() {
  }

  @Override
  public void run(String... args) throws Exception {
//    final TestEntity entity = new TestEntity(true, 25, 100.0, UUID.randomUUID(), "Test", false);
//    final TestEntity entity2 = new TestEntity(true, 25, 100.0, UUID.randomUUID(), "Test", false);
//    final TestEntity entity3 = new TestEntity(true, 25, 100.0, UUID.randomUUID(), "Test", false);
//
//    TestEntity saved = testEntityRepository.save(entity);
//    TestEntity saved2 = testEntityRepository.save(entity2);
//    TestEntity saved3 = testEntityRepository.save(entity3);
//    System.out.println("Entity saved: " + saved);
//
////    testEntityRepository.delete(saved);
////
////    System.out.println("Entity deleted: " + saved);
//
//    System.out.println("Entity count: " + testEntityRepository.count());
//    System.out.println("Entity exists: " + testEntityRepository.existsById(saved.getId()));


  }
}
